{
  "name": "kinetic-audio-synthesizer",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "homepage": "https://jhlee111.github.io/kinetic-audio-synthesizer/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "deploy:full": "./scripts/deploy.sh",
    "release": "./scripts/release.sh",
    "setup:hooks": "./scripts/setup-hooks.sh",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "clean": "rm -rf dist node_modules",
    "clean:install": "npm run clean && npm ci",
    "check:build": "npm run build && echo 'Build successful!'",
    "check:size": "du -sh dist/"
  },
  "dependencies": {
    "react-dom": "^18.2.0",
    "react": "^18.2.0",
    "@mediapipe/tasks-vision": "0.10.14",
    "tone": "14.7.77"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0",
    "gh-pages": "^6.1.1"
  }
}